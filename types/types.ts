export type frontMatter = {
  slug: string;
  frontMatter: any;
  title: string;
  date: string;
  description: string;
  image: string;
};

export type posts = {
  posts: string;
};

export type postcard = {
  image: string;
  title: string;
  date: string;
};

export type params = {};
