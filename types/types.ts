export type frontMatter = {
  frontMatter: any;
  title: string;
  date: string;
  description: string;
  image: string;
};

export type posts = {
  posts: string;
};

export type postcard = {
  image: string;
  title: string;
  date: string;
};
